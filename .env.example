PORT=
MONGODB_URI=
JWT_SECRET=

# Google OAuth — get this from Google Cloud Console → APIs & Services → Credentials
# Create an OAuth 2.0 Client ID (Web application type)
# Add your frontend origin to "Authorized JavaScript origins"
# You do NOT need the client secret for this flow (ID token verification only)
GOOGLE_CLIENT_ID=
